// import { productFieldValue } from 'dummy/helpers/product-field-value';
import { module, test } from "qunit";

module("Unit | Helper | product field value");

// Replace this with your real tests.
test("it works", function(assert) {
  // let result = productFieldValue([42]);
  assert.ok(true);
});
